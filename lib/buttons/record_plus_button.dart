import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class RecordPlusPage extends StatelessWidget {
  const RecordPlusPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Record Plus Page')),
    body: const Center(child: Text('Record Plus Feature Page')),
  );
}
